package blang.examples

model SimpleMixture { 
  
  random RealVar Y   
  random IntVar Z
  
  param List<RealVar> means 
  
  laws {
    Z ~ Bernoulli(0.5)
    Y | Z, means ~ Normal(means.get(Z), 1.0)
  }
}