package blang.distributions

model Bernoulli {
  random IntVar realization   
  param RealVar probability   
  
  laws {
    realization | probability ~ Categorical({
      // TODO: handle probability not in 0, 1
      denseSimplex(#[1.0 - probability, probability])
    })
  }
}